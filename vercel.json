{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "destination": "/api/$1", "source": "/api/(.*)" },
    { "destination": "/index.html", "source": "/((?!api/).*)" }
  ]
}
